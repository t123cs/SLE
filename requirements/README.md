# Requirements

- `sde.txt`, `lrd.txt`, and `pce.txt` list the core dependencies for each
  component.
- `lrd.freeze.txt` is the original environment snapshot kept for closer
  reproduction of the LRD setup.
